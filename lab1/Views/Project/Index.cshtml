@model TRS.Models.ViewModels.ProjectListModel
@{
    ViewData["Title"] = "Przegląd projektów";
}

<main class="bd-main order-1 gap-3">
    <h1 class="display-6">Nadzorowane projekty</h1>
    <div class="text-center">
        <table class="table">
            <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Projects[0].Code)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Projects[0].Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Projects[0].Budget)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Projects[0].BudgetLeft)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Projects[0].Categories)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Projects[0].Active)
                </th>
                <th>
                    Akcje
                </th>
            </tr>
            </thead>
            <tbody>
            @for (var i = 0; i < Model.Projects.Length; i++)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(model => model.Projects[i].Code)
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.Projects[i].Name)
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.Projects[i].Budget)
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.Projects[i].BudgetLeft)
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.Projects[i].Categories)
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.Projects[i].Active)
                    </td>
                    <td>
                        <a asp-controller="Project" asp-action="Show" asp-route-id="@Model.Projects[i].Code">Obejrzyj</a>
                        @if (Model.Projects[i].Active)
                        {
                            <a asp-controller="Project" asp-action="Edit" asp-route-id="@Model.Projects[i].Code">Zmień</a>
                            <form method="POST">
                                <button asp-controller="Project" asp-action="Close" asp-route-id="@Model.Projects[i].Code">Zamknij</button>
                            </form>
                        }
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
    <p>
        <a asp-controller="Project" asp-action="Add">Dodaj</a>
    </p>
</main>
